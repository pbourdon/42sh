ctrl + c sur heredoc annule le heredoc (pour le moment sa raffiche le heredoc)
+check si commit de Patricio gere bien le hse64 /dev/urandom | head -c 1000 | 
grep 42 | wc -l | sed -e 's/1/Yes/g' -e 's/0/No/g
eredoc


^ string1 ^ string2 ^
Quick substitution. Repeat the last command, replacing string1 with string2. Equivalent to ''!!:s/string1/string2/'' (see Modifiers below).
comprends pas comment sa marche


Si le number de l'event est superieur a un int, il affiche un nombre random

